\documentclass{article}
\usepackage{amsmath}
\usepackage{algorithm}
\usepackage{pdflscape}
\usepackage[noend]{algpseudocode}

\makeatletter
\def\BState{\State\hskip-\ALG@thistlm}
\makeatother

\begin{document}
\pagenumbering{gobble}
\begin{landscape}
\begin{algorithm}
\caption{A* Search Algorithm (Graph)}\label{euclid}
\begin{algorithmic}
\Function{A*}{start, goal}
	\State $\textit{closedset} \gets \textit{the empty set}$
	\State $\textit{openset} \gets  start $	
	\State $\textit{came\_from} \gets \text{the empty map}\textit{string}$
	\BState
	
	\State $\textit{q\_score}[start] \gets 0 $
	\State $\textit{f\_score}[start] \gets \textit{q\_score}[start] + \textit{heuristic\_cost\_estimate}(start, goal)$
	\BState
	
	\While{$openset \neq \emptyset$} 
		\State $\textit{current } \gets \text{the node in} \textit{ openset } \text{having the lowest } \textit{f\_score}[] \text{value}$
		\BState

		\If{current = goal} 
		\Return $\textit{reconstruct\_path}(came\_from, goal)$
		\EndIf

		\State $\text{remove} \textit{ current } \text{from} \textit{ openset}$
		\State $\text{add} \textit{ current } \text{to} \textit{ closedset}$
		\BState

		\ForAll{$neighbour \in neighbour\_nodes(current)$}
			\If{ $ neighbour \in closedset$}
				\State $\emph{continue}$
			\EndIf
			\State $\textit{neighbour\_g\_score} \gets \textit{g\_score}[current] + \textit{dist\_between}(current, neighbour)$

			\If{ $\textit{neighbour } \text{not in }  \textit{openset } \parallel \textit{neighbour\_g\_score} < \textit{g\_score}[neighbour]$}
				\State $\textit{came\_from}[neighbour] \gets \textit{current}$
				\State $\textit{g\_score}[neighbour] \gets \textit{neighbour\_g\_score}$
				\State $\textit{f\_score}[neighbour] \gets \textit{g\_score}[neighbour] + \textit{heuristic\_cost\_estimate}(neighbour, goal)$
				\If{$ neighbour \notin openset $ }
					\State $add \textit{neighbour} to \textit{openset}$
				\EndIf
			\EndIf
		\EndFor
	\EndWhile
	\Return $failure$
\EndFunction
\end{algorithmic}
\end{algorithm}
\end{landscape}
\end{document}